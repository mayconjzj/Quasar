import { ReelsContent } from './ReelsContent';
import { ReelsRoot } from './ReelsRoot';
import { ReelsTitle } from './ReelsTitle';

export const Reels = {
  Root: ReelsRoot,
  Content: ReelsContent,
  Title: ReelsTitle
};
